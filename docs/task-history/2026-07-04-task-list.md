# 2026-07-04 Task History

## 17:04 | AgentToolbox | avatar-browse 발견성 IA 재설계 (RFC #1730 → PR #1851 스택 전체)

### JIRA Ticket

```text
[Title]
[AgentToolbox] avatar-browse 발견성(IA) 재설계 — 도메인 패세트 + AI 보조 태깅/중복정리 (RFC #1730 → PR #1851)

[Description]
> Background
- 배포 1주 만에 /avatar-browse 에 카드 15 · 역할 88 · 업무 112 가 등록, 공유 라이브러리 성격상 급증 추세.
- 근본 원인은 레이아웃이 아니라 정보구조(IA): 분류 메타데이터 부재 + 정렬 단일(updated_at) + 의미상 중복 미탐지.
- 기존 follow-up(#1657 그룹핑 · #1658 그래프)로는 4개 사용자 작업(인물찾기·역량탐색·적임자매칭·중복정리) 중 둘만 커버, 중복 문제 미해결.
- RFC #1730 을 올려 동료 3인 리뷰(#1744 UX · #1745 백엔드/데이터 · #1746 AI/거버넌스)를 거쳐 방향 확정 후 착수.

> Work performed
- RFC 설계·3인 리뷰 반영 문서화 (#1747, #1751, #1754) — soft-merge 우선, 단일 릴리즈+런타임 게이트, 결정적 분류기, 발견성 회귀 차단 원칙 확정.
- 실험실 per-user opt-in 인프라 구축 (#1740 → #1758) — 런타임 게이트 기반.
- 6개 Plan 으로 분해 후 스택형(stacked) 구현:
  - Plan 1 (#1761): 도메인 스키마(휴면)+flag 셸+정렬 축 추가.
  - Plan 2 (#1762): taxonomy SSOT + 결정적(temp=0) 분류기.
  - Plan 3 (#1764): 도메인 패세트 + 섹션 UI 핵심 슬라이스.
  - Plan 4a/4b (#1765, #1766): 도메인 seed + dup 스키마 + trgm, 분류/중복 워커 + 백필 커맨드.
  - Plan 5 (#1774, #1779, #1788, #1793): 생성 시점 도메인 추천(조각2) + dup review + soft-merge → revert UI → hard-merge/un-merge(config OFF 출시).
  - Plan 6: authoring 추천 UX.
- taxonomy 도메인 어휘 16 → 67(전체 SLSI 직무체계) 확장 (#1772 → #1773).
- 스킬 사용처(used-by) API + 강제 detach/삭제 흐름 (#1831 → #1834, #1833 → #1843).
- 로컬 E2E 원클릭 재현 도구 (#1826 → #1827), facet 데모 seed (#1845 → #1846).
- 후속 UX 폴리시: 중복정리 패널·패싯 카운트 개선(#1844 → #1847), 진행성 노출·패싯 칩 레일 재설계(#1848 → #1849), 필터 우선(flat) 전환 + 세로 간격 통일(#1851).

> Results
- 직계 병합 PR 24개(+ 카드전환 선행 3개), 파생 이슈/트래킹 15+개.
- 코드 규모: +26,381 / -1,065 라인, 367 파일 변경, 128 커밋.
- DB 마이그레이션 다수(0080·0088 등), soft/hard merge 가역성(merge_audit before-state) 설계 적용.
- 발견성 회귀 없이 도메인 패세트·정렬·중복정리 전 파이프라인 프로덕션 게이트 뒤로 안전 출시.

> Notes
- 작업 기간: 2026-06-29(월) RFC 착수 ~ 2026-07-04(토) 최종 PR 병합 = 6일 연속(주말 포함).
- 단일 UI 개선처럼 보이지만 실제로는 스키마·분류기(AI)·워커·거버넌스·가역 병합·i18n·seed·E2E 도구를 아우르는 풀스택 기능. 스택형 분해로 리뷰 가능 단위를 유지하면서 병렬 진행.
- 자기보고 AI 메트릭(부분 집계, 노이즈 있음): 확보된 8개 PR 합계 약 63 human-h — 미기재 PR 다수라 하한선으로만 참고.
```

### PR

```markdown
## Title
avatar-browse 발견성 IA 재설계 (RFC #1730 → PR #1851, 24 PR 스택)

## Summary
- avatar-browse 를 레이아웃 문제가 아닌 정보구조(IA) 문제로 재정의하고, 도메인 패세트 + AI 보조 태깅/중복정리 파이프라인을 6개 Plan 스택으로 구현.
- RFC #1730 → 3인 리뷰 → 단일 릴리즈 + 런타임 게이트 전략으로 발견성 회귀·비가역 작업 리스크를 통제하며 출시.

## Changes
- `docs/superpowers/specs|plans`: RFC 설계 + 3인 리뷰 반영 + 6-Plan 분해 (#1747, #1751, #1754)
- `apps/server` (schema·taxonomy·worker): 도메인 스키마·결정적 분류기·seed(16→67)·dup 워커·백필 (#1761, #1762, #1765, #1766, #1773)
- `apps/web` (browse UI): 도메인 패세트·섹션→flat 필터 우선·정렬·중복정리 패널·패싯 칩 레일 (#1764, #1847, #1849, #1851)
- `apps/server`+`apps/web` (dedup): dup review·soft-merge·revert UI·hard-merge/un-merge(config OFF) (#1774, #1779, #1788, #1793)
- lab opt-in 인프라 + 스킬 used-by/detach + E2E 재현 도구 + facet 데모 seed (#1758, #1834, #1843, #1827, #1846)

## Test plan
- [x] 각 Plan PR별 typecheck / unit test / i18n 파리티 통과 후 병합
- [x] PG-gated 워커·마이그레이션 테스트(로컬 E2E 재현 도구 #1827)
- [x] hard-merge config OFF(soft-merge only)로 출시 후 신뢰 확보 시 지연 활성 검증
- [ ] 프로덕션 런타임 게이트 토글 ON 후 발견성/정렬/필터 회귀 모니터링
```

---

## 21:31 | AgentToolbox | avatar-browse IA 재설계 — 07-07 연속 작업(owner: dEitY719, #1852 → #1908) + 스코프 정정

> 이 항목은 위 07-04 항목(PR #1851까지)의 **연속(continuation)** 기록이며, 동시에 **스코프 정정**이다.
> 초안(이전 커밋)은 날짜 필터(`merged:>=2026-07-04`)로 조사해 팀 동료(msaltnet·jemings·JunNyung-Hur)의 PR(서브에이전트 export #1884/#1886, 대시보드 API #1858, 공개 OpenAPI CLI #1855, authoring 병합 #1860 등)까지 본인 작업으로 잘못 집계했다.
> 본 항목은 `--author dEitY719` (owner) 기준으로 재조사해 **본인이 저자인 PR만** 반영한다. 동료의 병렬 작업은 같은 기능의 팀 기여로 별도 표기.

### JIRA Ticket

```text
[Title]
[AgentToolbox] avatar-browse IA 재설계 07-07 연속 작업 (owner dEitY719, #1852 → #1908)

[Description]
> Background
- 07-04 항목은 RFC #1730 스택 1~6 Plan(본인 저자 24 PR, PR #1851까지)을 다뤘다. 07-05~07-06 에는 본인 저자 PR이 없고(동료가 export·대시보드·authoring 병렬 진행), 07-07 에 본인 작업이 집중 재개됨.
- 본인(dEitY719)이 저자인 PR만 owner 기준으로 재검색해, 07-07 작업 14건(+ 07-04 tail #1852)을 근거로 정리.
- 이전 초안의 과다 집계(동료 PR 포함, "30 PR/+22.5k") 정정: 본인 저자 연속분은 14 PR(자동생성 #1881 제외) · 실코드 +2,849/-859.

> Work performed (owner dEitY719, 07-07 중심)
- 필터 바 · 도메인 확인 UX 집중 반복(본인 07-07 작업의 핵심, 14건 중 9건이 이 계열):
  - authoring 도메인 확인 단계를 flat 2단 칩 DomainChipRail 로 통일(#1879, +249/-498 — 순삭 우위, 구조 단순화).
  - 토글·패싯을 공용 BrowseFilterBar 컴포넌트로 통합(#1880, +786/-114).
  - 필터 바 패싯을 토글 행 우측 인라인 트리거로 배치(#1887) → 필터 바+도메인 확인 UX 폴리시(#1896) → 빠른 필터 토글 3개를 탭 행 → 아래 필터 행으로 이동한 F-3 회귀 수정(#1900) — 위치·정렬·가독성 반복 튜닝.
  - 도메인 확인 단계 역할/업무 색상 코딩 + Domain/Specialty 티어 제목(#1889), 패싯 패널 대분류·세부 분야 티어 제목(#1902) — 색상·계층 표기 개선.
  - 패싯 선택 정합성 버그 수정: 도메인 해제 시 자식 전문분야 유령 선택 제거(cascade, #1901), domain+specialty 동시 선택 시 specialty 로 좁힘(#1908).
- DB/데이터 안전:
  - 도메인 백필을 workflow_dispatch CI 로 실행(#1903, +524): dry-run → 승인 → apply 3단 게이트로 프로덕션 백필 리스크 통제.
- 이식·인프라·문서:
  - 도메인/전문분야 사내 이식 런북 + avatar-rehearsal.sh 검증 스크립트(#1890, +376).
  - understand-anything apps 지식·도메인 그래프 + 온보딩·API 계약 맵 산출(#1881, 자동 생성 79k 라인 — 코드 델타 집계 제외).
  - dup-detection fake 스택 기본 ON(#1882), .githooks/pre-commit 을 프로젝트 소유 shim 으로 정리(#1906).
- 07-04 tail: hard-merge 거부(403/409/503) 피드백 배너가 뜨도록 수정(#1852).

> Results (owner dEitY719)
- 본인 저자 연속 병합 PR: 14건(#1852 + 07-07 13건, 자동생성 #1881 제외 / 포함 시 15건).
- 실코드 규모(연속분, #1881 제외): +2,849 / -859 라인, 61 파일.
- 07-07 본인 PR 14건 중 9건(#1879·1880·1887·1889·1896·1900·1901·1902·1908)이 browse 필터바/도메인확인/패싯 UX·정합성 계열 — "메뉴 위치·색상·가독성" 반복 투자의 직접 근거.

> Team context (동료 병렬 작업 — 본인 저자 아님, 참고용)
- 같은 기능 위에서 동료가 병렬로: 서브에이전트 번들 export(#1884/#1886, msaltnet), 대시보드 카드 API(#1858, msaltnet), 공개 OpenAPI CLI(#1855, jemings), authoring 병합 신뢰성(#1860, JunNyung-Hur), 상태/미사용 필터(#1876/#1877, msaltnet) 등을 진행. → 이는 팀 기여이며 본 owner 기록에는 카운트하지 않음.

> Notes
- 전체 기능 타임라인(owner dEitY719 기준): avatar-browse 230명 규모 대응·검색·탭 UI(06-23~06-25, #1654·1659·1669·1672·1674·1690) → 카드 전환 탭 UI(06-29, #1722·1726·1727) → RFC #1730 발견성 IA 스택(06-30~07-04, 24 PR) → 07-07 필터바/도메인확인 UX 반복 + 백필 CI(14 PR). 본인 avatar-browse 착수(06-23)부터 최종(07-07)까지 약 15일(휴일 제외 실작업 ~14일).
- owner 기준 본인 저자 avatar 관련 PR 합계(정정): RFC 스택 24 + 07-07 연속 14 = 38 PR(+ 06-23~06-29 browse 선행 ~9 PR). 실코드 누계 약 +29,230 / -1,924, 428 파일.
- "별 기능 없이 오래 걸렸다"는 인식과 실제의 괴리는 유효: 표면상 browse 한 화면이지만 (1) 결정적 AI 분류기·중복정리, (2) 가역 soft/hard merge 감사, (3) 도메인 taxonomy 16→67 확장, (4) 픽셀/색상/위치/가독성까지 맞춘 필터바·도메인확인 UX 반복(07-07 만 9 PR), (5) dry-run→승인→apply 3단 백필 CI 로 통제한 DB 마이그레이션 — 을 본인이 직접 수행.
- 자기보고 AI 메트릭(부분 집계·노이즈): footer 부착 PR만 부분 확인(#1880 1.5h·#1889 8h·#1896 4h·#1890 3h 등). 미기재 다수라 하한선으로만 참고.
```

### PR

```markdown
## Title
avatar-browse IA 재설계 07-07 연속 작업 (owner dEitY719, #1852→#1908) + 스코프 정정

## Summary
- 초안이 날짜 필터로 동료 PR(export·대시보드·OpenAPI·authoring)까지 본인 작업으로 과다 집계한 것을 `--author dEitY719` owner 기준으로 정정.
- 본인 저자 07-07 연속 작업 14 PR(#1852 + 07-07 13건, 자동생성 #1881 제외)을 반영 — 필터바/도메인확인 UX 반복 9건 + 백필 CI + 사내 이식 런북 중심.
- 동료의 병렬 작업은 "Team context" 로 분리 표기(본인 카운트에서 제외).

## Changes
- `docs/task-history/2026-07-04-task-list.md`: 연속 항목을 owner 기준으로 재작성 — PR 수/코드 델타/타임라인 정정, 동료 병렬 작업 분리, append-only(07-04 항목 보존)

## 근거 (agent-toolbox, owner dEitY719)
- `apps/web` (browse UX 반복): DomainChipRail 통일(#1879), 공용 BrowseFilterBar 통합(#1880), 인라인 트리거(#1887), UX 폴리시(#1896), F-3 회귀 수정(#1900), 색상/티어(#1889/#1902), 패싯 정합성(#1901/#1908)
- `.github/workflows` (data): 도메인 백필 dry-run→승인→apply CI (#1903)
- `docs`/scripts: 사내 이식 런북 + avatar-rehearsal.sh (#1890)
- infra: understand 그래프/온보딩 맵(#1881, 자동생성), dup fake ON(#1882), githooks shim(#1906)

## Test plan
- [x] owner 기준 재검색: `gh pr list --author dEitY719 --state all` 로 저자 PR만 확인
- [x] 코드 델타 재산출: 동료 PR·자동생성 #1881 제외 후 +2,849/-859 / 61 파일
- [x] 각 PR typecheck / unit / i18n 파리티 / PG-gated 통과 후 병합
- [x] 도메인 백필 CI dry-run → 승인 → apply 경로 검증(#1903)
- [ ] 프로덕션 런타임 게이트 ON 후 발견성/정렬/필터 회귀 모니터링
```

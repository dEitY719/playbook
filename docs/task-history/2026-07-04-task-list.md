# 2026-07-04 Task History

## 17:04 | AgentToolbox | avatar-browse 발견성 IA 재설계 (RFC #1730 → PR #1851 스택 전체)

### JIRA Ticket

```text
[Title]
[AgentToolbox] avatar-browse 발견성(IA) 재설계 — 도메인 패세트 + AI 보조 태깅/중복정리 (RFC #1730 → PR #1851)

[Description]
> Background
- 배포 1주 만에 /avatar-browse 에 카드 15 · 역할 88 · 업무 112 가 등록, 공유 라이브러리 성격상 급증 추세.
- 근본 원인은 레이아웃이 아니라 정보구조(IA): 분류 메타데이터 부재 + 정렬 단일(updated_at) + 의미상 중복 미탐지.
- 기존 follow-up(#1657 그룹핑 · #1658 그래프)로는 4개 사용자 작업(인물찾기·역량탐색·적임자매칭·중복정리) 중 둘만 커버, 중복 문제 미해결.
- RFC #1730 을 올려 동료 3인 리뷰(#1744 UX · #1745 백엔드/데이터 · #1746 AI/거버넌스)를 거쳐 방향 확정 후 착수.

> Work performed
- RFC 설계·3인 리뷰 반영 문서화 (#1747, #1751, #1754) — soft-merge 우선, 단일 릴리즈+런타임 게이트, 결정적 분류기, 발견성 회귀 차단 원칙 확정.
- 실험실 per-user opt-in 인프라 구축 (#1740 → #1758) — 런타임 게이트 기반.
- 6개 Plan 으로 분해 후 스택형(stacked) 구현:
  - Plan 1 (#1761): 도메인 스키마(휴면)+flag 셸+정렬 축 추가.
  - Plan 2 (#1762): taxonomy SSOT + 결정적(temp=0) 분류기.
  - Plan 3 (#1764): 도메인 패세트 + 섹션 UI 핵심 슬라이스.
  - Plan 4a/4b (#1765, #1766): 도메인 seed + dup 스키마 + trgm, 분류/중복 워커 + 백필 커맨드.
  - Plan 5 (#1774, #1779, #1788, #1793): 생성 시점 도메인 추천(조각2) + dup review + soft-merge → revert UI → hard-merge/un-merge(config OFF 출시).
  - Plan 6: authoring 추천 UX.
- taxonomy 도메인 어휘 16 → 67(전체 SLSI 직무체계) 확장 (#1772 → #1773).
- 스킬 사용처(used-by) API + 강제 detach/삭제 흐름 (#1831 → #1834, #1833 → #1843).
- 로컬 E2E 원클릭 재현 도구 (#1826 → #1827), facet 데모 seed (#1845 → #1846).
- 후속 UX 폴리시: 중복정리 패널·패싯 카운트 개선(#1844 → #1847), 진행성 노출·패싯 칩 레일 재설계(#1848 → #1849), 필터 우선(flat) 전환 + 세로 간격 통일(#1851).

> Results
- 직계 병합 PR 24개(+ 카드전환 선행 3개), 파생 이슈/트래킹 15+개.
- 코드 규모: +26,381 / -1,065 라인, 367 파일 변경, 128 커밋.
- DB 마이그레이션 다수(0080·0088 등), soft/hard merge 가역성(merge_audit before-state) 설계 적용.
- 발견성 회귀 없이 도메인 패세트·정렬·중복정리 전 파이프라인 프로덕션 게이트 뒤로 안전 출시.

> Notes
- 작업 기간: 2026-06-29(월) RFC 착수 ~ 2026-07-04(토) 최종 PR 병합 = 6일 연속(주말 포함).
- 단일 UI 개선처럼 보이지만 실제로는 스키마·분류기(AI)·워커·거버넌스·가역 병합·i18n·seed·E2E 도구를 아우르는 풀스택 기능. 스택형 분해로 리뷰 가능 단위를 유지하면서 병렬 진행.
- 자기보고 AI 메트릭(부분 집계, 노이즈 있음): 확보된 8개 PR 합계 약 63 human-h — 미기재 PR 다수라 하한선으로만 참고.
```

### PR

```markdown
## Title
avatar-browse 발견성 IA 재설계 (RFC #1730 → PR #1851, 24 PR 스택)

## Summary
- avatar-browse 를 레이아웃 문제가 아닌 정보구조(IA) 문제로 재정의하고, 도메인 패세트 + AI 보조 태깅/중복정리 파이프라인을 6개 Plan 스택으로 구현.
- RFC #1730 → 3인 리뷰 → 단일 릴리즈 + 런타임 게이트 전략으로 발견성 회귀·비가역 작업 리스크를 통제하며 출시.

## Changes
- `docs/superpowers/specs|plans`: RFC 설계 + 3인 리뷰 반영 + 6-Plan 분해 (#1747, #1751, #1754)
- `apps/server` (schema·taxonomy·worker): 도메인 스키마·결정적 분류기·seed(16→67)·dup 워커·백필 (#1761, #1762, #1765, #1766, #1773)
- `apps/web` (browse UI): 도메인 패세트·섹션→flat 필터 우선·정렬·중복정리 패널·패싯 칩 레일 (#1764, #1847, #1849, #1851)
- `apps/server`+`apps/web` (dedup): dup review·soft-merge·revert UI·hard-merge/un-merge(config OFF) (#1774, #1779, #1788, #1793)
- lab opt-in 인프라 + 스킬 used-by/detach + E2E 재현 도구 + facet 데모 seed (#1758, #1834, #1843, #1827, #1846)

## Test plan
- [x] 각 Plan PR별 typecheck / unit test / i18n 파리티 통과 후 병합
- [x] PG-gated 워커·마이그레이션 테스트(로컬 E2E 재현 도구 #1827)
- [x] hard-merge config OFF(soft-merge only)로 출시 후 신뢰 확보 시 지연 활성 검증
- [ ] 프로덕션 런타임 게이트 토글 ON 후 발견성/정렬/필터 회귀 모니터링
```

---

## 21:31 | AgentToolbox | avatar-browse IA 재설계 — 연속 배포(#1852 → #1908) + 전체 14일 타임라인 정정

> 이 항목은 위 07-04 항목(PR #1851까지, "6일" 구간)의 **연속(continuation)** 기록이다. 이후 agent-toolbox 에 추가 병합된 PR #1852 ~ #1908 을 반영하고, 기능 전체의 실질 개발 기간을 정정한다.

### JIRA Ticket

```text
[Title]
[AgentToolbox] avatar-browse IA 재설계 연속 배포 — export·대시보드 API·상태 필터·필터바 통합·백필 CI (#1852 → #1908, 전체 ~14일)

[Description]
> Background
- 07-04 항목은 RFC #1730 스택의 1~6 Plan(6일 구간, PR #1851까지)만 다뤘고, 이후 3일(07-04 오후 ~ 07-07)간 폴리시·확장·이식 준비가 연속 병합됨.
- "단순 UI 개선처럼 보이나 오래 걸렸다"는 인식을 정정하기 위해, #1851 이후 실제 병합된 30개 PR과 커밋을 재조사해 근거로 정리.
- 기반이 된 avatar-system 은 설계 문서(ADR 0007/0008·Phase 0)가 2026-06-14, 백엔드 Phase 1 이 06-15 에 착수 — RFC #1730(06-29)은 그 위에 올린 발견성(IA) 재설계 스택.

> Work performed
- 신규 역량 — 아바타 카드 → Claude Code 서브에이전트 번들 export:
  - Phase 1 백엔드(#1884, +2,500라인): 순수 렌더러·그래프 dataclass·결정적(deterministic) ZIP 번들 조립·export 전용 스킬 쿼리·태스크 게이트(active 재확인으로 §3.3 누출 차단), 실험실 opt-in 게이트.
  - Phase 2 프론트(#1886): 카드 상세 '내보내기' 버튼 + i18n + 테스트, 실험실 게이트.
- 대시보드/외부 통합 표면:
  - 대시보드 아바타 카드 목록/필터/페이지네이션 조회 API(#1858, +1,497) + 상세 조립 공유 헬퍼 추출.
  - 공유용 루트 skills/ 폴더 신설·agent-factory-api 이동·dashboard-api 스킬 추가(#1856, #1905).
  - 공개 OpenAPI 스키마 export CLI(#1855, +4,311) + drift required-check 무관 PR 블록 수정(#1865).
- avatar-authoring 병합 신뢰성(#1860): 구조 도구 move_task/delete_task/delete_role 로 병합을 id 기반 확정 연산화, edit_draft 실패 시 유사 원문 스니펫 힌트, prod edge SSE 타임아웃 600s.
- 발견성 폴리시 확장:
  - 상태 필터(등록/초안/미사용) 3분할 칩 + 미사용(상위 미연결) 항목 필터 확장(#1876, +1,221) — 순수 필터 헬퍼(filterByStatus·isRoleIsolated·isTaskIsolated).
  - 초안 경고 아이콘·상태 표기 등록/초안 통일·툴팁(#1867, #1877), 역할명 수정 후 카드 요약 즉시 갱신(#1866).
- UX 정밀 반복(사용자 경험 집중 투자 구간) — 필터 바/도메인 확인:
  - 토글·패싯을 공용 BrowseFilterBar 컴포넌트로 통합(#1880), authoring 도메인 확인을 flat 2단 칩 DomainChipRail 로 통일(#1879).
  - 도메인 확인 단계 역할/업무 색상 코딩 + Domain/Specialty 티어 제목(#1889), 패싯 패널 대분류·세부 분야 티어 제목(#1902).
  - 토글/정렬 드롭다운 배치를 여러 차례 미세 조정: 상태 칩 우측·검색 우측 MenuSelect(#1893) → 인라인 트리거(#1887) → 필터 바+도메인 확인 UX 폴리시(#1896) → 탭 행 → 필터 행 이동 F-3 회귀 수정(#1900) → 세로 간격 12px 리듬 통일(선행 #1851) — 위치·색상·간격·가독성 반복 튜닝.
  - 도메인 해제 시 자식 전문분야 유령 선택 제거(cascade, #1901), domain+specialty 동시 선택 시 specialty 로 좁힘(#1908) — 패싯 선택 정합성 버그 수정.
- DB/데이터 안전:
  - 도메인 백필을 workflow_dispatch CI 로 실행(#1903): dry-run → 승인 → apply 3단 게이트로 프로덕션 백필 리스크 통제.
  - content_updated_at 컬럼·백필 마이그레이션(0088) 등 스키마 변경을 가역/단계적으로 반영.
- 이식·문서: 도메인/전문분야 사내 이식 런북 + avatar-rehearsal.sh 검증 스크립트(#1890), teardown dev-server 하드 가드, AI-Agent 공개 API 가이드 ko/en(#1892, #1897), 프리셋 아바타 소개/등록 가이드 프로모 영상 씬 재구성(#1782 계열).

> Results
- 연속 병합 PR 30개(#1852 ~ #1908, avatar 스코프; VoC·understand-graph 제외).
- 코드 규모(연속 구간): 약 +22,500 / -2,400 라인, 약 320 파일 변경(자동 생성 understand-graph #1881 79k 라인은 집계 제외).
- 커밋: 07-04 09:00 이후 136 커밋. 일자별 활동 — 06-22 24 · 06-23 44 · 06-24 46 · 06-29 42 · 07-02 58 · 07-06 41 · 07-07 33 (주말 포함 거의 매일).
- 신규 기능 표면 3종 추가(서브에이전트 export·대시보드 카드 API·공개 OpenAPI CLI)와 발견성 UX 정밀화가 발견성/정렬/필터 회귀 없이 게이트 뒤로 안전 출시.

> Notes
- 실질 개발 기간 정정: avatar-system 설계·백엔드 착수(2026-06-14~15) → RFC #1730 발견성 IA 스택(06-29~07-04) → 연속 폴리시·확장·이식(07-06~07-07). 07-04 항목의 "6일"은 RFC #1730 스택 구간만 센 값이며, 기반 설계·백엔드와 연속 배포를 포함한 기능 전체 실질 기간은 약 2주(≈14일)다.
- "별 기능 추가 안 됐는데 오래 걸렸다"는 인식과 실제의 괴리: 표면상 한 화면(browse)의 개선처럼 보이나, 내부적으로는 (1) 결정적 AI 분류기·중복정리, (2) 가역 soft/hard merge 감사(merge_audit), (3) 서브에이전트 번들 export, (4) 대시보드·공개 OpenAPI 통합 표면, (5) 픽셀/색상/위치/가독성까지 맞춘 UX 반복(필터 바만 6+ PR), (6) dry-run→승인→apply 3단 백필 CI 로 통제한 DB 마이그레이션 — 을 아우르는 풀스택 작업.
- 자기보고 AI 메트릭(부분 집계, 노이즈 있음): footer 가 부착된 8개 PR 합계 약 29 human-h(#1851 8h 포함). 30개 PR 중 다수가 미기재라 실제 투입의 하한선으로만 참고.
```

### PR

```markdown
## Title
avatar-browse IA 재설계 연속 배포 (#1852 → #1908, 30 PR) + 14일 타임라인 정정

## Summary
- RFC #1730 스택(≤#1851, 07-04) 이후 07-04 오후~07-07 사이 연속 병합된 30개 PR을 재조사해 task-history 를 갱신.
- 서브에이전트 번들 export·대시보드 카드 API·공개 OpenAPI CLI 등 신규 표면 3종과, 필터 바 통합·상태 필터·도메인 확인 색상/티어 등 UX 정밀화, dry-run→승인→apply 백필 CI를 포함.
- 기능 전체 실질 개발 기간을 "6일" → 약 2주(≈14일)로 정정(설계·백엔드 06-14~ + 연속 배포 ~07-07 반영).

## Changes
- `docs/task-history/2026-07-04-task-list.md`: 07-04 항목의 연속 기록 추가 — #1852~#1908 반영 + 전체 타임라인 정정, append-only(기존 항목 보존)

## 근거 (agent-toolbox, 문서 대상 아님)
- `apps/server` (avatar-export): 순수 렌더러·결정적 ZIP 번들·export 스킬 쿼리·태스크 게이트 (#1884)
- `apps/web` (avatar-export): 카드 상세 내보내기 버튼 + i18n + 테스트 (#1886)
- `apps/server` (dashboard/openapi): 카드 목록/필터 API (#1858), 공개 OpenAPI export CLI (#1855)
- `apps/web` (browse UX): 공용 BrowseFilterBar 통합 (#1880), 상태/미사용 필터 (#1876), 도메인 확인 색상·티어 (#1889/#1902), 필터 바 배치 반복 (#1887/#1893/#1896/#1900), 패싯 정합성 (#1901/#1908)
- `apps/server` (authoring): id 기반 확정 병합 구조 도구 + SSE 600s (#1860)
- `.github/workflows` (data): 도메인 백필 dry-run→승인→apply CI (#1903)
- `docs`/scripts: 사내 이식 런북 + avatar-rehearsal.sh (#1890), AI-Agent 공개 API 가이드 ko/en (#1892/#1897)

## Test plan
- [x] 각 연속 PR별 typecheck / unit test / i18n 파리티 / PG-gated 테스트 통과 후 병합
- [x] export 결정적 ZIP 재현성·태스크 게이트 active 재확인 검증(#1884)
- [x] 도메인 백필 CI dry-run 리포트 검토 → 승인 → apply 경로 검증(#1903)
- [x] avatar-rehearsal.sh 로 도메인/전문분야 사내 이식 리허설(#1890)
- [ ] 프로덕션 런타임 게이트 ON 후 발견성/정렬/필터/export 회귀 모니터링
```
